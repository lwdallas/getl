package row_test

import (
	"testing"
	//"row"
)

func TestBasicFieldObjectCreation(t *testing.T) {
	const name, schema, table, typeval = "Name", "Schema", "Table", "Type"
	//var trow row.Row

	if false {
		t.Errorf("Field failed basic object creation")
	}
}
